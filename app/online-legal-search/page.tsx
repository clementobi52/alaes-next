'use client'

import { useMemo, useState } from 'react'
import { CheckCircle2, FileSearch, LockKeyhole, Search, ShieldCheck, Sparkles } from 'lucide-react'
import { LEGAL_SEARCH_RECORDS } from '@/lib/legal-search-data'

type Stage = 'search' | 'payment' | 'success'

export default function OnlineLegalSearchPage() {
  const [query, setQuery] = useState('')
  const [stage, setStage] = useState<Stage>('search')
  const [selectedId, setSelectedId] = useState<string | null>(null)
  const [paymentComplete, setPaymentComplete] = useState(false)
  const results = useMemo(() => {
    const value = query.trim().toLowerCase()
    if (!value) return []
    return LEGAL_SEARCH_RECORDS.filter((record) => [record.fileNo, record.owner, record.property, record.location, record.landUse].some((field) => field.toLowerCase().includes(value)))
  }, [query])
  const selected = LEGAL_SEARCH_RECORDS.find((record) => record.id === selectedId)

  return <main className="min-h-screen bg-background text-foreground">
    <header className="border-b border-border bg-card/90"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-5 lg:px-8"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary text-primary-foreground"><FileSearch className="h-5 w-5" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">ALAES Online</p><p className="font-semibold">Abia State Legal Search</p></div></div><span className="hidden items-center gap-2 text-sm text-muted-foreground sm:flex"><ShieldCheck className="h-4 w-4 text-primary" /> Public record search</span></div></header>
    <div className="mx-auto max-w-6xl px-5 py-10 lg:px-8 lg:py-16"><div className="grid gap-10 lg:grid-cols-[1.1fr_.9fr] lg:items-start"><section><span className="inline-flex items-center gap-2 rounded-full border border-primary/25 bg-primary/10 px-3 py-1.5 text-xs font-medium text-primary"><Sparkles className="h-3.5 w-3.5" /> Individual search portal</span><h1 className="mt-5 max-w-2xl text-4xl font-semibold tracking-tight text-balance sm:text-5xl">Search Abia property records online.</h1><p className="mt-5 max-w-xl text-base leading-7 text-muted-foreground">Find publicly searchable property information across Aba, Umuahia, Ohafia and other Abia locations. A detailed search report is available through a simulated payment checkout.</p><div className="mt-8 flex flex-wrap gap-4 text-sm text-muted-foreground"><span className="inline-flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-primary" /> Public search</span><span className="inline-flex items-center gap-2"><CheckCircle2 className="h-4 w-4 text-primary" /> Abia records</span><span className="inline-flex items-center gap-2"><LockKeyhole className="h-4 w-4 text-primary" /> Secure report flow</span></div></section><section className="rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between gap-3"><div><h2 className="text-lg font-semibold">Online Legal Search</h2><p className="mt-1 text-sm text-muted-foreground">Search by file number, owner, or location.</p></div><span className="rounded-full bg-muted px-3 py-1 text-xs text-muted-foreground">Free search</span></div>{stage === 'search' && <><div className="mt-6 flex gap-2"><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="e.g. LABA/10482 or Aba North" className="min-w-0 flex-1 rounded-lg border border-input bg-background px-3 py-2.5 text-sm outline-none ring-primary focus:ring-2" /><button type="button" className="inline-flex items-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground"><Search className="h-4 w-4" /> Search</button></div>{query && <div className="mt-5 space-y-3">{results.length ? results.map((record) => <button type="button" key={record.id} onClick={() => setSelectedId(record.id)} className={`block w-full rounded-xl border p-4 text-left transition-colors hover:border-primary/50 ${selectedId === record.id ? 'border-primary bg-primary/5' : 'border-border'}`}><div className="flex items-start justify-between gap-3"><div><p className="font-mono text-sm font-semibold text-primary">{record.fileNo}</p><p className="mt-1 font-medium">{record.property}</p><p className="mt-1 text-xs text-muted-foreground">{record.owner} · {record.location}</p></div><span className="rounded-full bg-muted px-2.5 py-1 text-xs">{record.status}</span></div></button>) : <p className="rounded-xl border border-dashed border-border p-5 text-center text-sm text-muted-foreground">No matching Abia records found. Try a file number, owner, or location.</p>}</div>}{selected && <div className="mt-5 rounded-xl bg-muted/50 p-4"><p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Selected record</p><div className="mt-3 grid gap-2 text-sm sm:grid-cols-2"><p><span className="text-muted-foreground">File:</span> {selected.fileNo}</p><p><span className="text-muted-foreground">Land use:</span> {selected.landUse}</p><p><span className="text-muted-foreground">Instrument:</span> {selected.instrument}</p><p><span className="text-muted-foreground">Updated:</span> {selected.lastUpdated}</p></div><button type="button" onClick={() => setStage('payment')} className="mt-5 w-full rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Request detailed report · ₦5,000</button></div>}</>}{stage === 'payment' && selected && <div className="mt-6"><div className="rounded-xl border border-primary/20 bg-primary/5 p-4"><p className="text-sm font-semibold">Detailed report request</p><p className="mt-1 text-sm text-muted-foreground">{selected.fileNo} · {selected.property}</p></div><div className="mt-5 grid gap-4"><label className="grid gap-1.5 text-sm font-medium">Email for report<input type="email" placeholder="you@example.com" className="h-10 rounded-lg border border-input bg-background px-3 text-sm" /></label><label className="grid gap-1.5 text-sm font-medium">Card number <input defaultValue="4242 4242 4242 4242" className="h-10 rounded-lg border border-input bg-background px-3 font-mono text-sm" /></label><div className="grid grid-cols-2 gap-3"><input defaultValue="12/30" aria-label="Expiry" className="h-10 rounded-lg border border-input bg-background px-3 text-sm" /><input defaultValue="123" aria-label="CVC" className="h-10 rounded-lg border border-input bg-background px-3 text-sm" /></div><button type="button" onClick={() => { setPaymentComplete(true); setStage('success') }} className="rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Simulate payment · ₦5,000</button><button type="button" onClick={() => setStage('search')} className="text-sm text-muted-foreground hover:text-foreground">Back to results</button></div></div>}{stage === 'success' && selected && <div className="mt-6 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-primary" /><h3 className="mt-4 text-xl font-semibold">Payment simulated successfully</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Your detailed Abia State search report for <span className="font-mono text-foreground">{selected.fileNo}</span> is ready.</p><button type="button" disabled={!paymentComplete} onClick={() => window.print()} className="mt-6 w-full rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Download detailed report</button><button type="button" onClick={() => { setStage('search'); setSelectedId(null); setQuery('') }} className="mt-3 text-sm text-muted-foreground hover:text-foreground">Start another search</button></div>}</section></div></div><footer className="border-t border-border py-6 text-center text-xs text-muted-foreground">Abia State Land Administration and E-Governance System · Online Legal Search</footer>
  </main>
}
