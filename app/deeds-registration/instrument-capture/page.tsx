'use client'

import { useMemo, useState } from 'react'
import { AppShell } from '@/components/app-shell'
import { ArrowUp, FilePlus2, MoreHorizontal, Plus, Search, X } from 'lucide-react'

const instruments = [
  { file: 'LUAC/AB/3518/AB', grantor: 'Abia State Ministry of Lands', grantee: 'Amina Okafor', type: 'Deed of Assignment', date: '2026-09-08', plot: 'Plot 15, Aba GRA', status: 'Verified' },
  { file: 'LUAC/AB/3517/UM', grantor: 'Chukwuemeka Nwosu', grantee: 'Uche Properties Ltd', type: 'Deed of Mortgage', date: '2026-09-06', plot: 'Plot 22, Umuahia Layout', status: 'Pending' },
  { file: 'LUM/2893', grantor: 'Ifeoma Eze', grantee: 'Daniel Eze', type: 'Deed of Lease', date: '2026-09-02', plot: 'Block 4, Umuahia', status: 'Verified' },
]
const types = ['Power of Attorney', 'Deed of Mortgage', 'Deed of Assignment', 'Deed of Lease']

function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="flex flex-col gap-2 text-sm font-medium"><span>{label}</span>{children}</label> }
function Input(props: React.InputHTMLAttributes<HTMLInputElement>) { return <input {...props} className={`h-10 rounded-lg border border-input bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring ${props.className ?? ''}`} /> }

export default function InstrumentCapturePage() {
  const [rows, setRows] = useState(instruments)
  const [query, setQuery] = useState('')
  const [formOpen, setFormOpen] = useState(false)
  const [menuFile, setMenuFile] = useState<string | null>(null)
  const [receiptFile, setReceiptFile] = useState<File | null>(null)
  const [receiptOpen, setReceiptOpen] = useState(false)
  const [birOpen, setBirOpen] = useState(false)
  const [selected, setSelected] = useState<typeof instruments[number] | null>(null)
  const [form, setForm] = useState({ file: '', grantor: '', grantee: '', type: types[2], plot: '' })
  const filtered = useMemo(() => rows.filter((item) => `${item.file} ${item.grantor} ${item.grantee}`.toLowerCase().includes(query.toLowerCase())), [rows, query])
  const openFor = (file: string) => { const row = rows.find((item) => item.file === file) ?? null; setSelected(row); setMenuFile(null) }
  const submitCapture = (event: React.FormEvent) => { event.preventDefault(); setRows((current) => [{ ...form, date: new Date().toISOString().slice(0, 10), status: 'Pending' }, ...current]); setForm({ file: '', grantor: '', grantee: '', type: types[2], plot: '' }); setFormOpen(false) }

  return <AppShell title="Instrument Capture" subtitle="Register and manage land instruments."><main className="mx-auto flex max-w-7xl flex-col gap-6"><header className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-3"><div className="rounded-lg bg-primary p-3 text-primary-foreground"><FilePlus2 className="size-6" /></div><div><p className="text-sm font-medium uppercase tracking-widest text-muted-foreground">Deeds Registration</p><h1 className="text-3xl font-semibold tracking-tight">Instrument Capture</h1><p className="mt-1 text-muted-foreground">Register and manage land instruments.</p></div></div><button onClick={() => setFormOpen(true)} className="inline-flex h-10 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-semibold text-primary-foreground"><Plus className="size-4" />Capture New Instrument</button></header><section className="grid gap-4 md:grid-cols-4">{[['Total Instruments', rows.length.toLocaleString(), '12% increase'], ['Verified', rows.filter((item) => item.status === 'Verified').length.toString(), '75% of total'], ['Pending', rows.filter((item) => item.status === 'Pending').length.toString(), '25% of total'], ['This Month', '87', '8% increase']].map(([label, value, detail], index) => <div key={label} className="rounded-xl border border-border bg-card p-5 shadow-sm"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-semibold">{value}</p><p className={`mt-1 flex items-center gap-1 text-xs ${index === 0 || index === 3 ? 'text-primary' : 'text-muted-foreground'}`}>{(index === 0 || index === 3) && <ArrowUp className="size-3" />}{detail}</p></div>)}</section><section className="rounded-xl border border-border bg-card shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border p-4"><h2 className="text-lg font-semibold">Registered Instruments</h2><div className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search instruments" className="h-10 rounded-lg border border-input bg-background pl-9 pr-3 text-sm" /></div></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted/40 text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-4 py-3">File number</th><th className="px-4 py-3">Instrument</th><th className="px-4 py-3">Parties</th><th className="px-4 py-3">Plot / location</th><th className="px-4 py-3">Date</th><th className="px-4 py-3">Status</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-border">{filtered.map((item) => <tr key={item.file} className="hover:bg-muted/20"><td className="px-4 py-4 font-mono font-semibold text-primary">{item.file}</td><td className="px-4 py-4">{item.type}</td><td className="px-4 py-4"><p>{item.grantor}</p><p className="text-xs text-muted-foreground">to {item.grantee}</p></td><td className="px-4 py-4">{item.plot}</td><td className="px-4 py-4">{item.date}</td><td className="px-4 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${item.status === 'Verified' ? 'bg-primary/10 text-primary' : 'bg-amber-500/10 text-amber-700'}`}>{item.status}</span></td><td className="relative px-4 py-4 text-right"><button type="button" aria-label={`Actions for ${item.file}`} onClick={() => setMenuFile(menuFile === item.file ? null : item.file)} className="rounded-lg p-2 hover:bg-muted"><MoreHorizontal className="size-5" /></button>{menuFile === item.file && <div className="absolute right-4 top-12 z-30 w-56 rounded-xl border border-border bg-card p-1 text-left shadow-xl"><button onClick={() => openFor(item.file)} className="block w-full rounded-lg px-3 py-2 text-sm hover:bg-muted">Open</button><button onClick={() => openFor(item.file)} className="block w-full rounded-lg px-3 py-2 text-sm hover:bg-muted">Edit</button><button onClick={() => { setSelected(item); setReceiptOpen(true); setMenuFile(null) }} className="block w-full rounded-lg px-3 py-2 text-sm hover:bg-muted">Upload receipts</button><button onClick={() => { setSelected(item); setBirOpen(true); setMenuFile(null) }} className="block w-full rounded-lg px-3 py-2 text-sm hover:bg-muted">BIR verification</button></div>}</td></tr>)}</tbody></table></div></section></main>{formOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><form onSubmit={submitCapture} className="w-full max-w-2xl rounded-2xl border border-border bg-card p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-widest text-primary">Deeds Registration</p><h2 className="mt-1 text-xl font-semibold">Capture New Instrument</h2></div><button type="button" onClick={() => setFormOpen(false)} aria-label="Close"><X className="size-5" /></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><Field label="File number"><Input required value={form.file} onChange={(event) => setForm({ ...form, file: event.target.value })} placeholder="LUAC/AB/3520/AB" /></Field><Field label="Instrument type"><select value={form.type} onChange={(event) => setForm({ ...form, type: event.target.value })} className="h-10 rounded-lg border border-input bg-background px-3 text-sm">{types.map((type) => <option key={type}>{type}</option>)}</select></Field><Field label="Grantor"><Input required value={form.grantor} onChange={(event) => setForm({ ...form, grantor: event.target.value })} /></Field><Field label="Grantee"><Input required value={form.grantee} onChange={(event) => setForm({ ...form, grantee: event.target.value })} /></Field><Field label="Plot / location"><Input required value={form.plot} onChange={(event) => setForm({ ...form, plot: event.target.value })} /></Field></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setFormOpen(false)} className="rounded-lg border border-border px-4 py-2 text-sm">Cancel</button><button type="submit" className="rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Capture instrument</button></div></form></div>}{receiptOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><section className="w-full max-w-xl rounded-2xl border border-border bg-card p-6 shadow-2xl"><h2 className="text-xl font-semibold">Upload receipts and payment documents</h2><p className="mt-1 text-sm text-muted-foreground">{selected?.file} · Required for simulated BIR verification.</p><label className="mt-6 flex min-h-32 cursor-pointer flex-col items-center justify-center rounded-xl border border-dashed border-border p-5 text-center"><input type="file" multiple className="sr-only" onChange={(event) => setReceiptFile(event.target.files?.[0] ?? null)} /><span className="font-medium">Choose receipts, payment evidence, or tax documents</span><span className="mt-1 text-xs text-muted-foreground">{receiptFile?.name ?? 'PDF, JPG, or PNG'}</span></label><div className="mt-5 flex justify-end gap-3"><button onClick={() => setReceiptOpen(false)} className="rounded-lg border border-border px-4 py-2 text-sm">Cancel</button><button onClick={() => setReceiptOpen(false)} disabled={!receiptFile} className="rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground disabled:opacity-50">Save documents</button></div></section></div>}{birOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 p-4"><section className="w-full max-w-xl rounded-2xl border border-border bg-card p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-widest text-primary">Simulated BIR handshake</p><h2 className="mt-1 text-xl font-semibold">Verify {selected?.file}</h2></div><button onClick={() => setBirOpen(false)} aria-label="Close"><X className="size-5" /></button></div><div className="mt-6 grid gap-3"><div className="rounded-lg border border-border p-3"><p className="font-medium">Phone verification</p><p className="text-sm text-muted-foreground">OTP sent and verified for the instrument party.</p></div><div className="rounded-lg border border-border p-3"><p className="font-medium">Tax identity verification</p><p className="text-sm text-muted-foreground">Individual NIN-linked Tax ID or company CAC-linked Tax ID.</p></div><div className="rounded-lg border border-border p-3"><p className="font-medium">BIR / Central Pay / APRR</p><p className="text-sm text-muted-foreground">Simulated handshake ready to run after documents are uploaded.</p></div></div><button onClick={() => setBirOpen(false)} className="mt-6 w-full rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Run simulated verification</button></section></div>}</AppShell>
}
