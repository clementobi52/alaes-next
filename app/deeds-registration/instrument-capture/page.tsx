'use client'

import { useMemo, useState } from 'react'
import { AppShell } from '@/components/app-shell'
import { ArrowUp, Check, FilePlus2, MoreHorizontal, Plus, Search, X } from 'lucide-react'

const instruments = [
  { file: 'LUAC/AB/3518/AB', grantor: 'Abia State Ministry of Lands', grantee: 'Amina Okafor', type: 'Deed of Assignment', date: '2026-09-08', plot: 'Plot 15, Aba GRA', status: 'Verified' },
  { file: 'LUAC/AB/3517/UM', grantor: 'Chukwuemeka Nwosu', grantee: 'Uche Properties Ltd', type: 'Deed of Mortgage', date: '2026-09-06', plot: 'Plot 22, Umuahia Layout', status: 'Pending' },
  { file: 'LUM/2893', grantor: 'Ifeoma Eze', grantee: 'Daniel Eze', type: 'Deed of Lease', date: '2026-09-02', plot: 'Block 4, Umuahia', status: 'Verified' },
]
const instrumentTypes = [
  ['Power of Attorney', 'Grantor', 'Grantee'], ['Irrevocable Power of Attorney', 'Grantor', 'Grantee'], ['Deed of Mortgage', 'Mortgagor', 'Mortgagee'], ['Tripartite Mortgage', 'Mortgagor', 'Mortgagee'], ['Deed of Assignment', 'Assignor', 'Assignee'], ['Deed of Lease', 'Lessor', 'Lessee'], ['Deed of Sub-Lease', 'Sub-Lessor', 'Sub-Lessee'], ['Deed of Sub-Division', 'Subdivider', 'Beneficiary'], ['Deed of Merger', 'Merging Party', 'Receiving Party'], ['Deed of Surrender', 'Surrenderer', 'Recipient'], ['Deed of Assent', 'Executor/Administrator', 'Beneficiary'], ['Deed of Release', 'Releasor', 'Releasee'],
]
const files = ['LUAC/AB/3518/AB', 'LUAC/AB/3517/UM', 'LUM/2893', 'LABA/2892', 'LUM/OH/2891']

function Field({ label, children, className = '' }: { label: string; children: React.ReactNode; className?: string }) { return <label className={`flex flex-col gap-2 text-sm font-medium ${className}`}><span>{label}</span>{children}</label> }
function Input(props: React.InputHTMLAttributes<HTMLInputElement>) { return <input {...props} className={`h-10 rounded-lg border border-input bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring ${props.className ?? ''}`} /> }
function Select(props: React.SelectHTMLAttributes<HTMLSelectElement>) { return <select {...props} className={`h-10 rounded-lg border border-input bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring ${props.className ?? ''}`} /> }
const Textarea = (props: React.TextareaHTMLAttributes<HTMLTextAreaElement>) => <textarea {...props} className={`min-h-20 rounded-lg border border-input bg-background px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-ring ${props.className ?? ''}`} />

export default function InstrumentCapturePage() {
  const [query, setQuery] = useState(''); const [showForm, setShowForm] = useState(false); const [showTypes, setShowTypes] = useState(false); const [openAction, setOpenAction] = useState<string | null>(null); const [actionFile, setActionFile] = useState<string | null>(null); const [showReceipts, setShowReceipts] = useState(false); const [receiptFiles, setReceiptFiles] = useState<File[]>([]); const [selectedType, setSelectedType] = useState(4); const [temporary, setTemporary] = useState(false); const [saved, setSaved] = useState(false); const [includeSurvey, setIncludeSurvey] = useState(false); const [showHandshake, setShowHandshake] = useState(false); const [handshake, setHandshake] = useState({ name: '', tin: '', amount: '25000' }); const [aprr, setAprr] = useState(''); const [nin, setNin] = useState(''); const [tinVerified, setTinVerified] = useState(false); const [taxpayerType, setTaxpayerType] = useState<'individual' | 'company'>('individual')
  const filtered = useMemo(() => instruments.filter((item) => `${item.file} ${item.grantor} ${item.grantee}`.toLowerCase().includes(query.toLowerCase())), [query])
  const type = instrumentTypes[selectedType]
  return <AppShell title="Instrument Capture" subtitle="Register and manage land instruments."><div className="mx-auto flex max-w-7xl flex-col gap-6"><header className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-3"><div className="rounded-lg bg-primary p-3 text-primary-foreground"><FilePlus2 className="size-6" /></div><div><p className="text-sm font-medium uppercase tracking-widest text-muted-foreground">Deeds Registration</p><h1 className="text-3xl font-semibold tracking-tight">Instrument Capture</h1><p className="mt-1 text-muted-foreground">Register and manage land instruments.</p></div></div><button onClick={() => setShowTypes(true)} className="inline-flex h-10 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-semibold text-primary-foreground"><Plus className="size-4" />Capture New Instrument</button></header><section className="grid gap-4 md:grid-cols-4">{[['Total Instruments','1,284','12% increase'],['Verified','968','75% of total'],['Pending','316','25% of total'],['This Month','87','8% increase']].map(([label,value,detail], index) => <div key={label} className="rounded-xl border border-border bg-card p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-semibold">{value}</p><p className={`mt-1 flex items-center gap-1 text-xs ${index === 0 || index === 3 ? 'text-primary' : 'text-muted-foreground'}`}>{(index === 0 || index === 3) && <ArrowUp className="size-3" />}{detail}</p></div><div className="rounded-full bg-primary/10 p-2 text-primary"><FilePlus2 className="size-5" /></div></div></div>)}</section><section className="rounded-xl border border-border bg-card shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border p-4"><h2 className="text-lg font-semibold">Registered Instruments</h2><div className="flex items-center gap-2"><div className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search instruments..." className="h-10 w-64 rounded-lg border border-input bg-background pl-9 pr-3 text-sm" /></div><button className="h-10 rounded-lg border border-input px-3 text-sm">Export</button></div></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted"><tr>{['File No','Grantor','Grantee','Transaction','Entry Date','Plot','Actions'].map((heading) => <th key={heading} className="px-4 py-3 font-medium text-muted-foreground">{heading}</th>)}</tr></thead><tbody>{filtered.map((item) => <tr key={item.file} className="border-t border-border hover:bg-muted/50"><td className="px-4 py-3 font-mono text-primary">{item.file}</td><td className="px-4 py-3">{item.grantor}</td><td className="px-4 py-3">{item.grantee}</td><td className="px-4 py-3"><span className="rounded-full bg-primary/10 px-2 py-1 text-xs text-primary">{item.type}</span></td><td className="px-4 py-3">{item.date}</td><td className="px-4 py-3">{item.plot}</td><td className="relative px-4 py-3"><button type="button" aria-label={`Actions for ${item.file}`} aria-expanded={openAction === item.file} onClick={() => setOpenAction(openAction === item.file ? null : item.file)} className="rounded-lg p-2 text-muted-foreground transition hover:bg-muted hover:text-foreground"><MoreHorizontal className="size-5" /></button>{openAction === item.file && <div className="absolute right-4 top-12 z-30 w-64 rounded-xl border border-border bg-card p-2 text-sm shadow-xl"><div className="border-b border-border px-3 py-2"><p className="font-semibold">Instrument actions</p><p className="mt-0.5 truncate font-mono text-xs text-muted-foreground">{item.file}</p></div><button type="button" onClick={() => setOpenAction(null)} className="mt-1 flex w-full rounded-lg px-3 py-2 text-left hover:bg-muted">Open</button><button type="button" onClick={() => setOpenAction(null)} className="flex w-full rounded-lg px-3 py-2 text-left hover:bg-muted">Edit</button><button type="button" onClick={() => { setActionFile(item.file); setShowReceipts(true); setOpenAction(null) }} className="flex w-full rounded-lg px-3 py-2 text-left hover:bg-muted">Upload receipts</button><button type="button" onClick={() => { setActionFile(item.file); setShowHandshake(true); setOpenAction(null) }} className="flex w-full rounded-lg px-3 py-2 text-left font-medium text-primary hover:bg-primary/10">BIR verification</button></div>}</td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t border-border p-4 text-sm text-muted-foreground"><span>Showing {filtered.length} of 1,284 results</span><div className="flex gap-2"><button className="rounded-lg border border-input px-3 py-2" disabled>Previous</button><button className="rounded-lg border border-input px-3 py-2">Next</button></div></div></section></div>{showReceipts && <div className="fixed inset-0 z-50 flex items-center justify-center bg-foreground/50 p-4"><section role="dialog" aria-modal="true" className="w-full max-w-xl rounded-xl border border-border bg-card shadow-xl"><div className="flex items-start justify-between border-b border-border p-6"><div><p className="text-xs font-semibold uppercase tracking-widest text-primary">Payment evidence</p><h2 className="mt-1 text-xl font-bold">Upload receipts</h2><p className="mt-1 text-sm text-muted-foreground">Attach receipts, payment evidence, and tax documents for BIR review.</p><p className="mt-2 font-mono text-xs text-muted-foreground">{actionFile}</p></div><button type="button" onClick={() => setShowReceipts(false)} className="rounded-lg border border-border p-2" aria-label="Close upload receipts"><X className="size-4" /></button></div><div className="space-y-4 p-6"><label className="flex min-h-32 cursor-pointer flex-col items-center justify-center rounded-xl border-2 border-dashed border-border bg-muted/30 p-5 text-center hover:border-primary"><input type="file" multiple accept=".pdf,.jpg,.jpeg,.png" className="sr-only" onChange={(event) => setReceiptFiles(Array.from(event.target.files ?? []))} /><p className="font-medium">Choose receipts and tax documents</p><p className="mt-1 text-xs text-muted-foreground">PDF, JPG, JPEG, or PNG</p></label>{receiptFiles.length > 0 && <div className="rounded-lg border border-border p-3 text-sm"><p className="font-medium">{receiptFiles.length} file{receiptFiles.length === 1 ? '' : 's'} selected</p>{receiptFiles.map((file) => <p key={file.name} className="mt-1 truncate text-xs text-muted-foreground">{file.name}</p>)}</div>}<div className="flex justify-end gap-2"><button type="button" onClick={() => setShowReceipts(false)} className="rounded-lg border border-border px-4 py-2 text-sm">Cancel</button><button type="button" disabled={receiptFiles.length === 0} onClick={() => setShowReceipts(false)} className="rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground disabled:opacity-50">Save documents for BIR review</button></div></div></section></div>}{showTypes && <div className="fixed inset-0 z-50 flex items-center justify-center bg-foreground/50 p-4"><section className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-xl border border-border bg-card shadow-xl"><div className="flex items-start justify-between border-b border-border p-6"><div><h2 className="text-xl font-bold">Select Instrument Type</h2><p className="mt-1 text-sm text-muted-foreground">Choose the type of instrument you want to register.</p></div><button onClick={() => setShowTypes(false)} aria-label="Close"><X className="size-5" /></button></div><div className="grid gap-3 p-6">{instrumentTypes.map(([name, first, second], index) => <button key={name} onClick={() => { setSelectedType(index); setShowTypes(false); setShowForm(true) }} className={`rounded-lg border p-4 text-left transition-colors ${selectedType === index ? 'border-primary bg-primary/10' : 'border-border hover:bg-muted'}`}><p className="font-medium">{name}</p><p className="mt-1 text-sm text-muted-foreground">{first} → {second}</p></button>)}</div></section></div>}{showHandshake && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/60 p-4"><section className="max-h-[calc(100vh-2rem)] w-full max-w-2xl overflow-y-auto rounded-2xl border border-border bg-card p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-widest text-primary">BIR / Central Pay handshake</p><h2 className="mt-1 text-xl font-semibold">Verify before final registration</h2><p className="mt-1 text-sm text-muted-foreground">The captured instrument is paused until the simulated Abia tax verification completes.</p></div><button onClick={() => setShowHandshake(false)} aria-label="Close"><X className="size-5" /></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><Field label="Taxpayer name"><Input required value={handshake.name} onChange={(event) => setHandshake((current) => ({ ...current, name: event.target.value }))} placeholder="Chinedu Okafor" /></Field><Field label="NIN / Individual Tax ID"><Input required inputMode="numeric" maxLength={11} value={nin} onChange={(event) => { const value = event.target.value.replace(/\D/g, '').slice(0, 11); setNin(value); if (value.length === 11) { setHandshake((current) => ({ ...current, tin: value })); setTinVerified(true) } else { setTinVerified(false) } }} placeholder="Enter 11-digit NIN" />{nin.length > 0 && nin.length < 11 && <span className="text-xs text-muted-foreground">Enter {11 - nin.length} more digits.</span>}{tinVerified && <span className="text-xs text-primary">NIN verified. Unified Tax ID is available for BIR verification.</span>}</Field><Field label="Taxpayer identity"><select value={taxpayerType} onChange={(event) => { setTaxpayerType(event.target.value as 'individual' | 'company'); setTinVerified(false); setHandshake((current) => ({ ...current, tin: '' })) }} className="h-10 w-full rounded-lg border border-input bg-background px-3 text-sm"><option value="individual">Individual — NIN is the Tax ID</option><option value="company">Company — CAC number is the Tax ID</option></select><p className="mt-1 text-xs text-muted-foreground">For individuals, Nigeria&apos;s unified Tax ID is linked to the 11-digit NIN. Companies use their CAC-linked Tax ID.</p></Field><Field label={taxpayerType === 'individual' ? 'Tax ID linked to NIN (optional)' : 'Company Tax ID / CAC number (optional)'}><Input value={handshake.tin} onChange={(event) => { setTinVerified(false); setHandshake((current) => ({ ...current, tin: event.target.value.toUpperCase() })) }} placeholder="AB-TIN-000000" />{tinVerified && !handshake.tin && <div className="mt-2 rounded-lg border border-dashed border-border p-3"><p className="text-xs text-muted-foreground">No Tax ID supplied. For an individual, capture the 11-digit NIN linked to the verified phone number; the NIN becomes the unified Tax ID.</p><div className="mt-2 flex gap-2"><Input inputMode="numeric" maxLength={11} value={nin} onChange={(event) => setNin(event.target.value.replace(/\D/g, ''))} placeholder="11-digit NIN" /><button type="button" disabled={nin.length !== 11} onClick={() => { setHandshake((current) => ({ ...current, tin: `AB-TAX-${nin}` })); setTinVerified(true) }} className="shrink-0 rounded-lg bg-primary px-3 text-xs font-semibold text-primary-foreground disabled:opacity-50">Create TIN</button></div></div>}{tinVerified && <span className="text-xs text-primary">Unified Tax ID confirmed from the verified NIN.</span>}</Field><Field label="Registration / tax amount"><Input type="number" min="0" value={handshake.amount} onChange={(event) => setHandshake((current) => ({ ...current, amount: event.target.value }))} /></Field></div><button disabled={!tinVerified} onClick={() => { setTinVerified(true); setAprr(`APRR-AB-${new Date().getFullYear()}-${Math.floor(Math.random() * 900000 + 100000)}`) }} className="mt-6 rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-50">{!tinVerified ? 'Verify 11-digit NIN to continue' : 'Run BIR verification'}</button>{aprr && <div className="mt-6 grid gap-4"><div className="grid gap-3 sm:grid-cols-3">{['Abia JIRS','Central Pay','APRR Registry'].map((label) => <div key={label} className="rounded-xl border border-primary/20 bg-primary/10 p-4 text-sm"><p className="font-semibold">{label}</p><p className="mt-1 text-primary">Verified</p></div>)}</div><div className="rounded-xl border border-border bg-muted/30 p-4 text-sm"><p>{taxpayerType === 'individual' ? 'Unified Tax ID (NIN-linked)' : 'Company Tax ID (CAC-linked)'}: <strong>{handshake.tin || (taxpayerType === 'individual' ? `AB-TAX-${nin}` : 'CAC-TAX-ID')}</strong></p><p className="mt-1">Total payable: <strong>₦{Number(handshake.amount).toLocaleString()}</strong></p><p className="mt-1 font-mono">APRR Code: <strong>{aprr}</strong></p></div><div className="flex justify-end gap-3"><button onClick={() => { setShowHandshake(false); setShowForm(false) }} className="rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Complete registration</button></div></div>}</section></div>}{showForm && <div className="fixed inset-0 z-50 flex items-center justify-center bg-foreground/50 p-4"><section className="max-h-[94vh] w-full max-w-3xl overflow-y-auto rounded-xl border border-border bg-card shadow-xl"><div className="flex items-start justify-between border-b border-border p-6"><div><h2 className="text-xl font-bold">Register {type[0]}</h2><p className="mt-1 text-sm text-muted-foreground">Enter the details for the new instrument.</p></div><button onClick={() => setShowForm(false)} aria-label="Close"><X className="size-5" /></button></div><form className="flex flex-col gap-5 p-6" onSubmit={(event) => { event.preventDefault(); setSaved(true); setShowHandshake(true) }}><section className="rounded-lg border border-border bg-muted/40 p-4"><h3 className="mb-4 text-lg font-semibold">File Number</h3><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={temporary} onChange={(event) => setTemporary(event.target.checked)} />This application has no extant file number</label>{temporary ? <div className="mt-4"><Field label="Temporary File Number"><Input value="TEMP/AB/0001" readOnly /></Field></div> : <div className="mt-4 grid gap-4 md:grid-cols-2"><Field label="File Number Type"><Select defaultValue="abia"><option value="abia">Abia File Number</option><option>Statutory File Number</option></Select></Field><Field label="File Number"><Select defaultValue={files[0]}>{files.map((file) => <option key={file}>{file}</option>)}</Select></Field></div>}</section><section className="rounded-lg border border-border bg-muted/40 p-4"><h3 className="mb-4 text-lg font-semibold">Registration Details</h3><div className="grid gap-4 md:grid-cols-2"><Field label="Root Registration Number"><Input placeholder="Enter root registration number" /></Field><Field label="Registration Number"><Input placeholder="Enter registration number" /></Field><Field label="Registration Office"><Input defaultValue="Abia State Deeds Registry" /></Field><Field label="Registration Officer"><Input placeholder="Officer name" /></Field></div></section><section className="rounded-lg border border-border bg-muted/40 p-4"><h3 className="mb-4 text-lg font-semibold">{type[1]} Information</h3><div className="grid gap-4 md:grid-cols-2"><Field label={`${type[1]} Name`}><Input placeholder="Enter full name or organisation" /></Field><Field label="Phone Number"><Input type="tel" /></Field><Field label="Email Address"><Input type="email" /></Field><Field label="Street Address"><Input /></Field><Field label="City"><Input /></Field><Field label="State"><Input defaultValue="Abia" /></Field><Field label="Country"><Input defaultValue="Nigeria" /></Field><Field label="Postal Code"><Input /></Field></div></section><section className="rounded-lg border border-border bg-muted/40 p-4"><h3 className="mb-4 text-lg font-semibold">{type[2]} Information</h3><div className="grid gap-4 md:grid-cols-2"><Field label={`${type[2]} Name`}><Input placeholder="Enter full name or organisation" /></Field><Field label="Phone Number"><Input type="tel" /></Field><Field label="Email Address"><Input type="email" /></Field><Field label="Street Address"><Input /></Field><Field label="City"><Input /></Field><Field label="State"><Input defaultValue="Abia" /></Field><Field label="Country"><Input defaultValue="Nigeria" /></Field><Field label="Postal Code"><Input /></Field></div></section><section className="rounded-lg border border-border bg-muted/40 p-4"><h3 className="mb-4 text-lg font-semibold">Solicitor Information</h3><div className="grid gap-4 md:grid-cols-2"><Field label="Solicitor Name"><Input /></Field><Field label="Solicitor Phone"><Input type="tel" /></Field><Field label="Solicitor Email"><Input type="email" /></Field><Field label="Solicitor Address" className="md:col-span-2"><Textarea /></Field></div></section><section className="rounded-lg border border-border bg-muted/40 p-4"><h3 className="mb-4 text-lg font-semibold">Property Details</h3><div className="grid gap-4 md:grid-cols-2"><Field label="Plot Description" className="md:col-span-2"><Textarea placeholder="Plot description" /></Field><Field label="Plot Size"><Input placeholder="100 x 50 metres" /></Field><Field label="Land Use"><Select><option>Residential</option><option>Commercial</option><option>Agricultural</option><option>Industrial</option><option>Mixed Use</option></Select></Field><Field label="LGA"><Input /></Field><Field label="District"><Input /></Field><Field label="Plot Number"><Input /></Field><Field label="Block Number"><Input /></Field><Field label="Survey Plan Number"><Input /></Field></div><label className="mt-4 flex items-center gap-2 text-sm"><input type="checkbox" checked={includeSurvey} onChange={(event) => setIncludeSurvey(event.target.checked)} />Include Survey Information</label>{includeSurvey && <div className="mt-4 grid gap-4 md:grid-cols-3"><Field label="Surveyor"><Input /></Field><Field label="Survey Date"><Input type="date" /></Field><Field label="Beacon Numbers"><Input /></Field></div>}</section><section className="rounded-lg border border-border bg-muted/40 p-4"><h3 className="mb-4 text-lg font-semibold">Transaction and Instrument Details</h3><div className="grid gap-4 md:grid-cols-2"><Field label="Instrument Type"><Select value={type[0]} onChange={(event) => setSelectedType(instrumentTypes.findIndex(([name]) => name === event.target.value))}>{instrumentTypes.map(([name]) => <option key={name}>{name}</option>)}</Select></Field><Field label="Transaction Date"><Input type="date" /></Field><Field label="Consideration Amount"><Input type="number" min="0" placeholder="Amount" /></Field><Field label="Currency"><Select defaultValue="NGN"><option>NGN</option><option>USD</option></Select></Field><Field label="Receipt Number"><Input /></Field><Field label="Stamp Duty"><Input type="number" min="0" /></Field><Field label="Instrument Reference"><Input /></Field><Field label="Registration Date"><Input type="date" /></Field><Field label="Entry Date"><Input type="date" /></Field><Field label="Supporting Documents" className="md:col-span-2"><Input type="file" multiple accept="application/pdf,image/*" className="h-auto py-2" /></Field><Field label="Notes" className="md:col-span-2"><Textarea placeholder="Additional notes" /></Field></div></section><div className="flex justify-end gap-3 border-t border-border pt-4"><button type="button" onClick={() => setShowForm(false)} className="rounded-lg border border-input px-4 py-2 text-sm">Cancel</button><button type="submit" className="rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Submit Registration</button></div>{saved && <p className="flex items-center gap-2 text-sm font-medium text-primary"><Check className="size-4" />Instrument captured successfully.</p>}</form></section></div>}</AppShell>
}
