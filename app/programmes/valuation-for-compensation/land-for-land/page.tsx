'use client'

import { useMemo, useState } from 'react'
import { AppShell } from '@/components/app-shell'
import { MapPinned, Plus, Search, Trash2 } from 'lucide-react'

type Allocation = { id: number; beneficiary: string; lga: string; acreage: number; plot: string; status: string }
const initialAllocations: Allocation[] = [
  { id: 1, beneficiary: 'Chinedu Okafor', lga: 'Umuahia North', acreage: 2.4, plot: 'AB-LFL-001', status: 'Allocated' },
  { id: 2, beneficiary: 'Adaeze Nwosu', lga: 'Isiala Ngwa South', acreage: 1.8, plot: 'AB-LFL-002', status: 'Survey pending' },
  { id: 3, beneficiary: 'Emeka Eze', lga: 'Aba North', acreage: 3.1, plot: 'AB-LFL-003', status: 'Allocated' },
]
export default function LandForLandPage() {
  const [allocations, setAllocations] = useState(initialAllocations)
  const [query, setQuery] = useState('')
  const [form, setForm] = useState({ beneficiary: '', lga: 'Umuahia North', acreage: '', plot: '' })
  const filtered = useMemo(() => allocations.filter((row) => `${row.beneficiary} ${row.lga} ${row.plot}`.toLowerCase().includes(query.toLowerCase())), [allocations, query])
  const totalPlots = allocations.length + 3
  const farmerShare = Math.floor(totalPlots / 2)
  const governmentShare = Math.ceil(totalPlots / 2)
  function addAllocation() { if (!form.beneficiary || !form.acreage || !form.plot) return; setAllocations([...allocations, { id: Date.now(), beneficiary: form.beneficiary, lga: form.lga, acreage: Number(form.acreage), plot: form.plot, status: 'Survey pending' }]); setForm({ beneficiary: '', lga: 'Umuahia North', acreage: '', plot: '' }) }
  return <AppShell title="Land-for-Land Compensation" subtitle="Valuation for Compensation · Abia State"><div className="flex flex-col gap-6"><section className="grid gap-4 sm:grid-cols-4">{[['Total plots', totalPlots], ['Farmer allocation', farmerShare], ['Government allocation', governmentShare], ['Total acreage', `${allocations.reduce((sum, row) => sum + row.acreage, 0).toFixed(1)} ha`]].map(([label, value]) => <div key={label} className="rounded-xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>)}</section><section className="rounded-xl border border-border bg-card p-5"><div className="mb-4 flex items-center gap-2"><MapPinned className="size-5 text-primary" /><div><h2 className="text-lg font-semibold">Land allocation capture</h2><p className="text-sm text-muted-foreground">Farmer and government shares follow a 50:50 allocation model.</p></div></div><div className="grid gap-3 md:grid-cols-5"><input placeholder="Beneficiary" value={form.beneficiary} onChange={(e) => setForm({ ...form, beneficiary: e.target.value })} className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><select value={form.lga} onChange={(e) => setForm({ ...form, lga: e.target.value })} className="rounded-lg border border-input bg-background px-3 py-2 text-sm"><option>Umuahia North</option><option>Aba North</option><option>Isiala Ngwa South</option><option>Arochukwu</option></select><input type="number" step="0.1" placeholder="Acreage (ha)" value={form.acreage} onChange={(e) => setForm({ ...form, acreage: e.target.value })} className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><input placeholder="Plot number" value={form.plot} onChange={(e) => setForm({ ...form, plot: e.target.value })} className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><button onClick={addAllocation} className="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground"><Plus className="size-4" />Add allocation</button></div></section><section className="rounded-xl border border-border bg-card"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border p-5"><div><h2 className="text-lg font-semibold">Land-for-land register</h2><p className="text-sm text-muted-foreground">Replacement plots and survey status for affected Abia beneficiaries.</p></div><label className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search beneficiary, LGA, or plot" className="rounded-lg border border-input bg-background py-2 pl-9 pr-3 text-sm" /></label></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted"><tr>{['Beneficiary','LGA','Acreage','Plot number','Status','Action'].map((label) => <th key={label} className="px-5 py-3 font-semibold text-muted-foreground">{label}</th>)}</tr></thead><tbody>{filtered.map((row) => <tr key={row.id} className="border-t border-border"><td className="px-5 py-4 font-medium">{row.beneficiary}</td><td className="px-5 py-4">{row.lga}</td><td className="px-5 py-4">{row.acreage} ha</td><td className="px-5 py-4 font-mono">{row.plot}</td><td className="px-5 py-4"><span className="rounded-full bg-muted px-3 py-1 text-xs font-semibold">{row.status}</span></td><td className="px-5 py-4"><button aria-label={`Delete ${row.beneficiary}`} onClick={() => setAllocations(allocations.filter((item) => item.id !== row.id))} className="rounded-md p-2 text-destructive hover:bg-muted"><Trash2 className="size-4" /></button></td></tr>)}</tbody></table></div></section></div></AppShell>
}
