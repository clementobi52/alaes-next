'use client'

import { useMemo, useState } from 'react'
import { AppShell } from '@/components/app-shell'
import { Calculator, Plus, Search, Trash2 } from 'lucide-react'

type Tree = { id: number; beneficiary: string; tree: string; quantity: number; unit: number }
const initialTrees: Tree[] = [
  { id: 1, beneficiary: 'Chinedu Okafor', tree: 'Oil palm', quantity: 18, unit: 18500 },
  { id: 2, beneficiary: 'Adaeze Nwosu', tree: 'Cocoa', quantity: 42, unit: 12400 },
  { id: 3, beneficiary: 'Emeka Eze', tree: 'Mango', quantity: 11, unit: 9800 },
]
const naira = new Intl.NumberFormat('en-NG', { style: 'currency', currency: 'NGN', maximumFractionDigits: 0 })
export default function MonetaryCompensationPage() {
  const [trees, setTrees] = useState(initialTrees)
  const [query, setQuery] = useState('')
  const [form, setForm] = useState({ beneficiary: '', tree: 'Oil palm', quantity: '', unit: '' })
  const filtered = useMemo(() => trees.filter((row) => `${row.beneficiary} ${row.tree}`.toLowerCase().includes(query.toLowerCase())), [trees, query])
  const total = trees.reduce((sum, row) => sum + row.quantity * row.unit, 0)
  function addTree() { if (!form.beneficiary || !form.quantity || !form.unit) return; setTrees([...trees, { id: Date.now(), beneficiary: form.beneficiary, tree: form.tree, quantity: Number(form.quantity), unit: Number(form.unit) }]); setForm({ beneficiary: '', tree: 'Oil palm', quantity: '', unit: '' }) }
  return <AppShell title="Monetary Compensation" subtitle="Valuation for Compensation · Abia State"><div className="flex flex-col gap-6"><section className="grid gap-4 sm:grid-cols-4">{[['Beneficiaries', new Set(trees.map((row) => row.beneficiary)).size], ['Economic trees', trees.reduce((sum, row) => sum + row.quantity, 0)], ['Valuation lines', trees.length], ['Total valuation', naira.format(total)]].map(([label, value]) => <div key={label} className="rounded-xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-semibold">{value}</p></div>)}</section><section className="rounded-xl border border-border bg-card p-5"><div className="mb-4 flex items-center gap-2"><Calculator className="size-5 text-primary" /><div><h2 className="text-lg font-semibold">Economic tree valuation</h2><p className="text-sm text-muted-foreground">Quantity × unit rate; totals are calculated automatically.</p></div></div><div className="grid gap-3 md:grid-cols-5"><input placeholder="Beneficiary" value={form.beneficiary} onChange={(e) => setForm({ ...form, beneficiary: e.target.value })} className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><select value={form.tree} onChange={(e) => setForm({ ...form, tree: e.target.value })} className="rounded-lg border border-input bg-background px-3 py-2 text-sm"><option>Oil palm</option><option>Cocoa</option><option>Mango</option><option>Cashew</option></select><input type="number" placeholder="Quantity" value={form.quantity} onChange={(e) => setForm({ ...form, quantity: e.target.value })} className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><input type="number" placeholder="Unit rate (₦)" value={form.unit} onChange={(e) => setForm({ ...form, unit: e.target.value })} className="rounded-lg border border-input bg-background px-3 py-2 text-sm" /><button onClick={addTree} className="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground"><Plus className="size-4" />Add line</button></div></section><section className="rounded-xl border border-border bg-card"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border p-5"><div><h2 className="text-lg font-semibold">Monetary compensation register</h2><p className="text-sm text-muted-foreground">Abia State beneficiary valuation schedule.</p></div><label className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search beneficiary or tree" className="rounded-lg border border-input bg-background py-2 pl-9 pr-3 text-sm" /></label></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted"><tr>{['Beneficiary','Economic tree','Quantity','Unit rate','Line total','Action'].map((label) => <th key={label} className="px-5 py-3 font-semibold text-muted-foreground">{label}</th>)}</tr></thead><tbody>{filtered.map((row) => <tr key={row.id} className="border-t border-border"><td className="px-5 py-4 font-medium">{row.beneficiary}</td><td className="px-5 py-4">{row.tree}</td><td className="px-5 py-4">{row.quantity}</td><td className="px-5 py-4">{naira.format(row.unit)}</td><td className="px-5 py-4 font-semibold">{naira.format(row.quantity * row.unit)}</td><td className="px-5 py-4"><button aria-label={`Delete ${row.beneficiary}`} onClick={() => setTrees(trees.filter((item) => item.id !== row.id))} className="rounded-md p-2 text-destructive hover:bg-muted"><Trash2 className="size-4" /></button></td></tr>)}</tbody></table></div></section></div></AppShell>
}
