'use client'

import { useMemo, useState } from 'react'
import { Archive, ChevronLeft, ChevronRight, Download, Eye, FileText, FolderOpen, Plus, Search, Share2, Star, X } from 'lucide-react'

const files = [
  { id: 'FILE-2023-001', name: 'Alhaji Ibrahim Dantata', number: 'RES-86-2244', kangis: 'KNGP 00338', newKangis: 'KNO001', type: 'Certificate of Occupancy', size: '2.4 MB', format: 'PDF', date: '12 Mar 2026', accessed: 'Today, 09:42', by: 'System Admin', tags: ['Land Records', 'Residential'], pages: 6 },
  { id: 'FILE-2024-014', name: 'Umuahia North Layout Plan', number: 'LAY-24-0912', kangis: 'KNGP 00941', newKangis: 'KNO118', type: 'Layout Plan', size: '8.1 MB', format: 'PDF', date: '08 Mar 2026', accessed: 'Yesterday, 15:10', by: 'Survey Unit', tags: ['Survey', 'Layout'], pages: 12 },
  { id: 'FILE-2026-083', name: 'Aba Industrial Ventures Ltd', number: 'IND-26-0830', kangis: 'KNGP 01204', newKangis: 'KNO304', type: 'Deed of Assignment', size: '1.8 MB', format: 'PDF', date: '02 Mar 2026', accessed: '05 Mar 2026', by: 'Deeds Registry', tags: ['Legal Documents', 'Industrial'], pages: 4 },
  { id: 'FILE-2025-077', name: 'Chukwudi Okoro', number: 'RES-25-7718', kangis: 'KNGP 01077', newKangis: 'KNO241', type: 'Survey Plan', size: '3.6 MB', format: 'PDF', date: '26 Feb 2026', accessed: '01 Mar 2026', by: 'Survey Unit', tags: ['Survey', 'Residential'], pages: 3 },
]

export function FileDigitalLibrary() {
  const [query, setQuery] = useState('')
  const [category, setCategory] = useState('all')
  const [selected, setSelected] = useState<(typeof files)[number] | null>(null)
  const [starred, setStarred] = useState<string[]>([])

  const filtered = useMemo(() => files.filter((file) => {
    const matchesQuery = [file.name, file.number, file.kangis, file.newKangis, file.type].join(' ').toLowerCase().includes(query.toLowerCase())
    const matchesCategory = category === 'all' || file.tags.some((tag) => tag.toLowerCase().includes(category))
    return matchesQuery && matchesCategory
  }), [category, query])

  return <div className="space-y-6">
    <div className="grid gap-4 md:grid-cols-3">
      {[['1,248', 'Total Archived Files', 'Digitally archived files'], ['78', 'Recently Added', 'Added in the last 30 days'], ['4.2 GB', 'Storage Used', 'Of 10 GB allocated']].map(([value, label, detail]) => <section key={label} className="rounded-xl border border-border bg-card p-5 shadow-sm"><p className="text-sm font-medium text-muted-foreground">{label}</p><p className="mt-2 text-3xl font-bold tracking-tight">{value}</p><p className="mt-1 text-xs text-muted-foreground">{detail}</p></section>)}
    </div>

    <section className="rounded-xl border border-border bg-card shadow-sm">
      <div className="border-b border-border p-5"><h2 className="text-lg font-semibold">Search Archives</h2><p className="mt-1 text-sm text-muted-foreground">Find archived files by file number, title, or content.</p></div>
      <div className="flex flex-col gap-4 p-5 lg:flex-row lg:items-end"><label className="flex-1 text-sm font-medium">Search term<div className="relative mt-2"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Enter search term..." className="h-10 w-full rounded-lg border border-input bg-background pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-ring" /></div></label><label className="text-sm font-medium">Category<select value={category} onChange={(event) => setCategory(event.target.value)} className="mt-2 h-10 w-full rounded-lg border border-input bg-background px-3 text-sm lg:w-48"><option value="all">All Categories</option><option value="land records">Land Records</option><option value="legal documents">Legal Documents</option><option value="survey">Survey</option></select></label><button type="button" onClick={() => { setQuery(''); setCategory('all') }} className="h-10 rounded-lg border border-border px-4 text-sm hover:bg-muted">Clear</button></div>
    </section>

    <section className="overflow-hidden rounded-xl border border-border bg-card shadow-sm"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="text-lg font-semibold">Archived Files</h2><p className="text-sm text-muted-foreground">{filtered.length} files available in Doc-WARE</p></div><button type="button" className="inline-flex h-10 items-center gap-2 rounded-lg bg-primary px-4 text-sm font-semibold text-primary-foreground"><Plus className="size-4" /> Add file</button></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted/50 text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-5 py-3">File</th><th className="px-5 py-3">File Number</th><th className="px-5 py-3">Document Type</th><th className="px-5 py-3">Size</th><th className="px-5 py-3">Uploaded</th><th className="px-5 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-border">{filtered.map((file) => <tr key={file.id} className="hover:bg-muted/30"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-lg bg-primary/10 text-primary"><FileText className="size-5" /></span><div><p className="font-medium">{file.name}</p><p className="text-xs text-muted-foreground">{file.id} · {file.by}</p></div></div></td><td className="px-5 py-4 font-mono text-xs">{file.number}<p className="mt-1 text-[11px] text-muted-foreground">{file.kangis}</p></td><td className="px-5 py-4"><span className="rounded-full bg-muted px-2.5 py-1 text-xs">{file.type}</span></td><td className="px-5 py-4 text-muted-foreground">{file.size}</td><td className="px-5 py-4 text-muted-foreground">{file.date}</td><td className="px-5 py-4"><div className="flex justify-end gap-1"><button type="button" aria-label={`Star ${file.name}`} onClick={() => setStarred((current) => current.includes(file.id) ? current.filter((id) => id !== file.id) : [...current, file.id])} className="rounded-lg p-2 hover:bg-muted"><Star className={`size-4 ${starred.includes(file.id) ? 'fill-amber-400 text-amber-500' : 'text-muted-foreground'}`} /></button><button type="button" onClick={() => setSelected(file)} className="rounded-lg p-2 text-muted-foreground hover:bg-muted" aria-label={`Open ${file.name}`}><Eye className="size-4" /></button></div></td></tr>)}{filtered.length === 0 && <tr><td colSpan={6} className="p-12 text-center text-muted-foreground">No archived files match your search.</td></tr>}</tbody></table></div><div className="flex items-center justify-between border-t border-border p-4 text-sm text-muted-foreground"><span>Showing {filtered.length} of {files.length} files</span><div className="flex gap-2"><button type="button" className="rounded-lg border border-border p-2" aria-label="Previous page"><ChevronLeft className="size-4" /></button><button type="button" className="rounded-lg border border-border p-2" aria-label="Next page"><ChevronRight className="size-4" /></button></div></div></section>

    {selected && <div className="fixed inset-0 z-50 flex items-center justify-center bg-foreground/50 p-4" role="presentation" onClick={() => setSelected(null)}><section role="dialog" aria-modal="true" aria-labelledby="docware-dialog-title" onClick={(event) => event.stopPropagation()} className="flex max-h-[90vh] w-full max-w-5xl flex-col overflow-hidden rounded-2xl border border-border bg-card shadow-2xl"><div className="flex items-center justify-between border-b border-border p-5"><div><p className="text-xs font-semibold uppercase tracking-widest text-primary">Doc-WARE archive</p><h2 id="docware-dialog-title" className="mt-1 text-xl font-bold">{selected.name}</h2></div><button type="button" onClick={() => setSelected(null)} className="rounded-lg p-2 hover:bg-muted" aria-label="Close document details"><X className="size-5" /></button></div><div className="grid min-h-0 flex-1 overflow-y-auto md:grid-cols-[minmax(0,2fr)_minmax(280px,1fr)]"><div className="flex min-h-96 items-center justify-center bg-muted/40 p-8"><div className="flex aspect-[3/4] w-full max-w-md flex-col rounded-lg bg-background p-8 shadow-lg"><div className="flex items-center justify-between"><Archive className="size-8 text-primary" /><span className="rounded-full border border-primary/30 px-2 py-1 text-xs text-primary">{selected.format}</span></div><div className="my-auto space-y-5"><h3 className="text-2xl font-bold">{selected.type}</h3><div className="h-px bg-border" /><p className="text-sm leading-6 text-muted-foreground">Digital archive preview for {selected.name}. This document contains {selected.pages} indexed pages and is stored in the Doc-WARE repository.</p></div><div className="flex items-center justify-between border-t border-border pt-4 text-xs text-muted-foreground"><span>{selected.number}</span><span>{selected.pages} pages</span></div></div></div><div className="space-y-6 p-6"><div className="grid gap-4 sm:grid-cols-2 md:grid-cols-1"><div><p className="text-xs font-medium text-muted-foreground">File Number</p><p className="mt-1 font-mono text-sm">{selected.number}</p></div><div><p className="text-xs font-medium text-muted-foreground">KANGIS File No</p><p className="mt-1 font-mono text-sm">{selected.kangis}</p></div><div><p className="text-xs font-medium text-muted-foreground">New KANGIS File No</p><p className="mt-1 font-mono text-sm">{selected.newKangis}</p></div><div><p className="text-xs font-medium text-muted-foreground">Last accessed</p><p className="mt-1 text-sm">{selected.accessed}</p></div></div><div><p className="text-xs font-medium text-muted-foreground">Tags</p><div className="mt-2 flex flex-wrap gap-2">{selected.tags.map((tag) => <span key={tag} className="rounded-full border border-border px-2.5 py-1 text-xs">{tag}</span>)}</div></div><div className="flex flex-wrap gap-2"><button type="button" className="inline-flex items-center gap-2 rounded-lg border border-border px-3 py-2 text-sm hover:bg-muted"><Download className="size-4" /> Download</button><button type="button" className="inline-flex items-center gap-2 rounded-lg border border-border px-3 py-2 text-sm hover:bg-muted"><Share2 className="size-4" /> Share</button><button type="button" className="inline-flex items-center gap-2 rounded-lg bg-primary px-3 py-2 text-sm font-semibold text-primary-foreground"><FolderOpen className="size-4" /> View document</button></div></div></div></section></div>}
  </div>
}
