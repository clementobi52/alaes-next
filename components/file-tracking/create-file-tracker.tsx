'use client'

import { useEffect, useState } from 'react'
import { Clock3, Eye, FilePlus2, FileText, RefreshCw, Save, Search, Settings2 } from 'lucide-react'
import { offices } from '@/lib/file-tracking-data'

type CreateFileTrackerProps = {
  onCancel: () => void
  onSave: (event: React.FormEvent<HTMLFormElement>) => void
  onOpenLog: () => void
}

const fieldClass = 'mt-1 w-full rounded-md border border-border bg-background px-3 py-2 text-sm text-foreground outline-none transition placeholder:text-muted-foreground focus:border-primary focus:ring-2 focus:ring-primary/20'

function Field({ label, name, placeholder, required = false, children }: { label: string; name?: string; placeholder?: string; required?: boolean; children?: React.ReactNode }) {
  return <label className="flex min-w-0 flex-col text-xs font-medium text-foreground"><span>{label}{required && <span className="text-destructive"> *</span>}</span>{children ?? <input name={name} placeholder={placeholder} required={required} className={fieldClass} />}</label>
}

function Section({ icon: Icon, title, description, children }: { icon: typeof FileText; title: string; description: string; children: React.ReactNode }) {
  return <section className="overflow-hidden rounded-lg border border-border bg-card"><div className="border-b border-border px-5 py-4"><div className="flex items-center gap-2"><Icon className="size-4 text-primary" /><h2 className="font-semibold text-foreground">{title}</h2></div><p className="mt-1 text-xs text-muted-foreground">{description}</p></div><div className="p-5">{children}</div></section>
}

export function CreateFileTracker({ onCancel, onSave, onOpenLog }: CreateFileTrackerProps) {
  const [now, setNow] = useState(new Date())
  const [requestMode, setRequestMode] = useState<'manual' | 'digital'>('manual')
  useEffect(() => { const timer = window.setInterval(() => setNow(new Date()), 1000); return () => window.clearInterval(timer) }, [])
  const date = now.toISOString().slice(0, 10)
  const time = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit', second: '2-digit' })
  const logId = `LOG-${now.toISOString().replace(/[-:TZ.]/g, '').slice(0, 12)}-${String(now.getSeconds()).padStart(3, '0')}`

  return <main className="min-h-screen bg-background p-4 text-foreground lg:p-6"><div className="mx-auto max-w-[1320px] space-y-4"><div className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-border bg-card px-4 py-3"><div className="flex items-center gap-2 text-xs"><span className="mr-1 uppercase tracking-wider text-muted-foreground">Request mode</span><button type="button" onClick={() => setRequestMode('manual')} className={`rounded-md border px-3 py-1.5 font-medium ${requestMode === 'manual' ? 'border-primary bg-primary/10 text-primary' : 'border-transparent text-muted-foreground hover:bg-muted'}`}>Manual Request</button><button type="button" onClick={() => setRequestMode('digital')} className={`rounded-md border px-3 py-1.5 font-medium ${requestMode === 'digital' ? 'border-primary bg-primary/10 text-primary' : 'border-transparent text-muted-foreground hover:bg-muted'}`}>Digital Request</button></div><div className="flex gap-2"><button type="button" onClick={onCancel} className="inline-flex items-center gap-2 rounded-md border border-border px-3 py-2 text-xs text-foreground"><RefreshCw className="size-3.5" /> Reset Form</button><button type="button" className="inline-flex items-center gap-2 rounded-md border border-border px-3 py-2 text-xs text-foreground"><Eye className="size-3.5" /> Preview</button></div></div>{requestMode === 'digital' && <section className="rounded-lg border border-primary/40 bg-primary/10 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-semibold text-primary">Digital File Request</h2><p className="text-xs text-muted-foreground">Request a physical file to be sent to your office for approval.</p></div><button type="button" className="rounded-full border border-primary/40 px-3 py-1 text-xs text-primary">View All Requests</button></div><div className="mt-5 grid grid-cols-5 gap-2 text-center text-[10px] text-muted-foreground"><div><span className="mx-auto mb-1 flex size-7 items-center justify-center rounded-full bg-primary text-primary-foreground">✓</span>Search File</div><div><span className="mx-auto mb-1 flex size-7 items-center justify-center rounded-full bg-primary text-primary-foreground">✓</span>Details Auto-filled</div><div><span className="mx-auto mb-1 flex size-7 items-center justify-center rounded-full bg-primary text-primary-foreground">➤</span>Send Request</div><div><span className="mx-auto mb-1 flex size-7 items-center justify-center rounded-full bg-muted">4</span>Await Approval</div><div><span className="mx-auto mb-1 flex size-7 items-center justify-center rounded-full bg-muted">5</span>File Dispatched</div></div><p className="mt-3 text-xs text-muted-foreground">Requester: Current User · Your Department: Current Department</p></section>}<div className="rounded-lg border border-border bg-card"><div className="flex items-center gap-6 border-b border-border px-5 py-3 text-xs"><button type="button" className="inline-flex items-center gap-2 border-b-2 border-primary pb-3 font-medium text-primary"><FilePlus2 className="size-3.5" /> Create File Tracker</button><button type="button" className="inline-flex items-center gap-2 text-muted-foreground"><FileText className="size-3.5" /> <button type="button" onClick={onOpenLog} className="text-muted-foreground hover:text-primary">File Tracker Log</button> <span className="rounded-full bg-primary/15 px-2 py-0.5 text-primary">{0}</span></button></div><form onSubmit={onSave} className="grid gap-4 p-4 lg:grid-cols-[minmax(0,1fr)_212px] lg:p-5"><div className="flex min-w-0 flex-col gap-4"><Section icon={FileText} title="File Details" description="Enter the basic file information."><div className="grid gap-4 md:grid-cols-3"><Field label="File No" name="reference" placeholder="Loaded from Quick Search" required /><Field label="File Tracking ID" placeholder="Auto-generated after saving" /><Field label="Registry Code" placeholder="Derived from selected registry" /></div><div className="mt-4 grid gap-4 md:grid-cols-2"><Field label="File Title" name="subject" placeholder="Enter the file title" required /><Field label="Number of Pages" placeholder="Enter total pages" /></div><div className="mt-4 grid gap-4 md:grid-cols-2"><Field label="File Priority" name="priority"><select name="priority" defaultValue="Normal" className={fieldClass}><option>Normal</option><option>Urgent</option></select></Field><Field label="Status Update"><select name="status" defaultValue="In Transit" className={fieldClass}><option>In Transit</option><option>Received</option><option>Pending</option></select></Field></div><div className="mt-4 grid gap-4 md:grid-cols-2"><Field label="Request Purpose" placeholder="Select the reason this file is being requested" /><Field label="Timeline (Days)" name="timeline" placeholder="e.g. 5" /></div><Field label="Notes / Remarks" name="notes" placeholder="Reason why the file is in this office"><textarea name="notes" rows={3} placeholder="Reason why the file is in this office" className={fieldClass} /></Field></Section><Section icon={Settings2} title="Office Details" description="Select the current office location."><div className="mb-4 rounded-md border border-primary/30 bg-primary/5 px-3 py-2 text-xs text-muted-foreground">Some fields may fill automatically. Confirm the office details before saving the log.</div><div className="grid gap-4 md:grid-cols-2"><Field label="Registry (Origin)" name="sourceOffice"><select name="sourceOffice" defaultValue="" required className={fieldClass}><option value="">Select registry / origin</option>{offices.map((office) => <option key={office}>{office}</option>)}</select></Field><Field label="Requester Department" name="requesterDepartment"><select name="requesterDepartment" defaultValue="" className={fieldClass}><option value="">Select destination</option>{offices.map((office) => <option key={office}>{office}</option>)}</select></Field><Field label="Requester Director" placeholder="Select requester director" /><Field label="Requester Office" name="receivingOffice"><select name="receivingOffice" defaultValue="" required className={fieldClass}><option value="">Select receiving office</option>{offices.map((office) => <option key={office}>{office}</option>)}</select></Field><Field label="File Requester" name="receivingOfficer" placeholder="Select receiving officer" required /><Field label="Sending Officer" placeholder="Klaes Admin" /></div></Section><Section icon={FileText} title="File Log Session" description="Initial log entry details (auto-populated)."><div className="grid gap-4 md:grid-cols-2"><Field label="Log ID" placeholder={logId} /><Field label="Log In Time" placeholder={time} /><Field label="Log In Date" placeholder={date} /><Field label="Log Out Time" name="dueDate" placeholder={time} /><Field label="Log Out Date" placeholder={date} /></div></Section><div className="flex justify-end gap-2"><button type="button" onClick={onCancel} className="rounded-md border border-border px-4 py-2 text-sm text-foreground">Reset</button><button type="submit" className="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground"><Save className="size-4" /> {requestMode === 'digital' ? 'Send Request' : 'Save File Log'}</button></div></div><aside className="flex flex-col gap-4"><div className="rounded-lg border border-border bg-card"><div className="border-b border-border p-4"><div className="flex items-center gap-2 font-semibold"><Clock3 className="size-4 text-primary" /> Current Date &amp; Time</div><p className="mt-1 text-xs text-muted-foreground">Live timestamp for log entries</p></div><div className="p-4 text-center"><p className="font-mono text-2xl font-bold text-foreground">{time}</p><p className="text-xs text-muted-foreground">{date}</p><div className="my-4 border-t border-border" /><p className="text-left text-xs text-muted-foreground">File No: <strong className="ml-2 text-foreground">—</strong></p><p className="mt-3 text-left text-xs text-muted-foreground">Log ID: <strong className="ml-2 rounded bg-primary/10 px-2 py-1 font-mono text-primary">{logId}</strong></p></div></div><div className="rounded-lg border border-border bg-card p-4"><div className="flex items-center gap-2 font-semibold"><Search className="size-4 text-primary" /> Quick Actions</div><p className="mt-1 text-xs text-muted-foreground">Perform common file tracking operations</p><div className="mt-4 grid grid-cols-2 gap-2 text-center text-xs"><button type="button" className="rounded-md p-3 text-muted-foreground hover:bg-muted">Search Files</button><button type="button" className="rounded-md p-3 text-muted-foreground hover:bg-muted">Office List</button><button type="button" className="rounded-md p-3 text-muted-foreground hover:bg-muted">Track Status</button><button type="button" className="rounded-md p-3 text-muted-foreground hover:bg-muted">Assignment Center</button></div></div></aside></form></div></div></main>
}
